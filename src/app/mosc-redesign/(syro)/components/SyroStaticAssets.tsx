/**
 * Optional legacy Syro CSS from /mosc/assets. Omitted in event-site-manager when those
 * static files are not deployed — downloads and other mosc-redesign pages use Tailwind +
 * mosc-redesign-shell.css instead.
 */
export default function SyroStaticAssets() {
  return null;
}
