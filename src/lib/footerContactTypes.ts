/** Resolved tenant contact props for public footer / contact sections (v2.0). */
export type FooterContactProps = {
  formattedAddress: string | null;
  phoneNumber: string | null;
  email: string | null;
  websiteUrl: string | null;
  description: string | null;
};
